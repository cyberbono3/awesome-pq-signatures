[package]
name = "pq_bench"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Shared benchmark configuration for awesome-pq-signatures"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
sha2 = "0.10"
toml = "0.8"
